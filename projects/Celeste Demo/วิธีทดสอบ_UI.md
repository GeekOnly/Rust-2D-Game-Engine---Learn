# 🧪 วิธีทดสอบ UI ที่แก้ไขแล้ว

## ✅ การแก้ไขเสร็จสมบูรณ์

Build เสร็จแล้ว! ตอนนี้พร้อมทดสอบ

## 📋 ขั้นตอนการทดสอบ

### 1. รัน Engine

```bash
cargo run --release
```

### 2. เปิด Project

- Project: `Celeste Demo`
- Scene: `main.json` (ควรเปิดอยู่แล้ว)

### 3. กด Play

- คลิกปุ่ม **Play** (▶️) ใน Editor
- หรือกด **Space** บน keyboard

### 4. ดู Game View

ควรเห็น UI แสดงผลและ**อัพเดทค่าจริง**:

```
FPS: Updates: 60
FPS: Updates: 120
FPS: Updates: 180
...
```

### 5. ตรวจสอบ Console Logs

ควรเห็น logs แบบนี้:

```
[Lua] === SIMPLE HUD TEST: Update 60 ===
[Lua] UI table exists, calling set_text...
🔧 [Lua UI] set_text called: celeste_hud/fps_counter = 'Updates: 60'
🔧 [Lua UI] Queue size after push: 1
🔍 Checking UI commands: 1 commands in queue
🔍 Processing 1 UI commands
🔍 SetText: celeste_hud/fps_counter = 'Updates: 60'
```

**สิ่งที่สำคัญ:**
- ✅ `Queue size after push: 1` (ไม่ใช่ 0!)
- ✅ `Checking UI commands: 1 commands in queue` (ไม่ใช่ 0!)
- ✅ `Processing 1 UI commands`
- ✅ `SetText: celeste_hud/fps_counter = ...`

## 🎮 การทดสอบเพิ่มเติม

### ทดสอบ Player Movement

1. กด **A/D** หรือ **ลูกศร ซ้าย/ขวา** เพื่อเคลื่อนที่
2. ดู UI - ควรเห็น:
   - Position X เปลี่ยน
   - Velocity X เปลี่ยน

### ทดสอบ Jump

1. กด **Space** เพื่อกระโดด
2. ดู UI - ควรเห็น:
   - Position Y เปลี่ยน
   - Velocity Y เปลี่ยน

## ❌ ถ้ายังไม่ทำงาน

### ตรวจสอบ Logs

ถ้าเห็น:
```
🔍 Checking UI commands: 0 commands in queue
```

แสดงว่ายังมีปัญหา - แจ้งให้ทราบ

### ตรวจสอบ UI Instance

ใน Editor:
1. ดู **Hierarchy** panel
2. หา entity ที่มี script `hud_simple_update`
3. ตรวจสอบว่า script ถูก attach อยู่

### Force Rebuild

ถ้ายังไม่ทำงาน ลอง:

```bash
cargo clean
cargo build --release
cargo run --release
```

## 🎉 ผลลัพธ์ที่คาดหวัง

- ✅ UI แสดงผลใน Game View
- ✅ UI อัพเดทค่าทุก 1 วินาที (60 frames)
- ✅ เห็น logs ว่า commands ถูกเพิ่มเข้า queue
- ✅ เห็น logs ว่า commands ถูก process
- ✅ ค่าใน UI เปลี่ยนตามเวลา

## 📝 หมายเหตุ

- Script ที่ใช้: `hud_simple_update.lua`
- Entity: 284 (LDTK Layer entity)
- UI Instance: `celeste_hud`
- Update ทุก 60 frames (1 วินาที)

---

**สถานะ:** พร้อมทดสอบ ✅
**Build:** Release (optimized) ✅
**วันที่:** 8 ธันวาคม 2025
