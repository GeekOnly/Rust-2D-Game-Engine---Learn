# ✅ แก้ไข Layout ใน celeste_hud.uiprefab

## ปัญหา

ค่า `anchored_position` ใน prefab file ผิด ทำให้ UI elements แสดงตำแหน่งผิดพลาด

## สาเหตุ

ใน Unity coordinate system:
- **Anchored Position** คือระยะห่างจาก anchor point
- ถ้า anchor อยู่ **ด้านบน** (Y=1.0) → ต้องใช้ Y **ลบ** เพื่อเลื่อนลง
- ถ้า anchor อยู่ **ด้านล่าง** (Y=0.0) → ต้องใช้ Y **บวก** เพื่อเลื่อนขึ้น

## การแก้ไข

### 1. Top-Left Elements (anchor_min: [0.0, 1.0])

**ก่อนแก้ไข** (ผิด):
```json
"player_health": { "anchored_position": [20.0, 20.0] }    // Y บวก = ขึ้นไปด้านบน (นอกจอ)
"stamina_bar": { "anchored_position": [20.0, 50.0] }      // Y บวก = ขึ้นไปด้านบน (นอกจอ)
"dash_indicator": { "anchored_position": [20.0, 75.0] }   // Y บวก = ขึ้นไปด้านบน (นอกจอ)
```

**หลังแก้ไข** (ถูก):
```json
"player_health": { "anchored_position": [20.0, -20.0] }   // Y ลบ = ลงมาจากด้านบน ✅
"stamina_bar": { "anchored_position": [20.0, -50.0] }     // Y ลบ = ลงมาจากด้านบน ✅
"dash_indicator": { "anchored_position": [20.0, -75.0] }  // Y ลบ = ลงมาจากด้านบน ✅
```

### 2. Top-Right Elements (anchor_min: [1.0, 1.0])

**ก่อนแก้ไข** (ผิด):
```json
"position_debug": { "anchored_position": [-180.0, 10.0] }  // Y บวก = ขึ้นไปด้านบน (นอกจอ)
"velocity_debug": { "anchored_position": [-180.0, 35.0] }  // Y บวก = ขึ้นไปด้านบน (นอกจอ)
"fps_counter": { "anchored_position": [-100.0, 60.0] }     // Y บวก = ขึ้นไปด้านบน (นอกจอ)
```

**หลังแก้ไข** (ถูก):
```json
"position_debug": { "anchored_position": [-10.0, -10.0] }  // Y ลบ = ลงมาจากด้านบน ✅
"velocity_debug": { "anchored_position": [-10.0, -35.0] }  // Y ลบ = ลงมาจากด้านบน ✅
"fps_counter": { "anchored_position": [-10.0, -60.0] }     // Y ลบ = ลงมาจากด้านบน ✅
```

### 3. Bottom-Left Elements (anchor_min: [0.0, 0.0])

**ก่อนแก้ไข** (ผิด):
```json
"grounded_indicator": { "anchored_position": [20.0, -30.0] }   // Y ลบ = ลงไปด้านล่าง (นอกจอ)
"wall_slide_indicator": { "anchored_position": [20.0, -55.0] } // Y ลบ = ลงไปด้านล่าง (นอกจอ)
```

**หลังแก้ไข** (ถูก):
```json
"grounded_indicator": { "anchored_position": [20.0, 30.0] }    // Y บวก = ขึ้นมาจากด้านล่าง ✅
"wall_slide_indicator": { "anchored_position": [20.0, 55.0] }  // Y บวก = ขึ้นมาจากด้านล่าง ✅
```

### 4. Center Elements (anchor_min: [0.5, 0.5])

**ก่อนแก้ไข** (ผิด):
```json
"dashing_indicator": { "anchored_position": [-50.0, -100.0] }  // ออกจากกึ่งกลาง
```

**หลังแก้ไข** (ถูก):
```json
"dashing_indicator": { "anchored_position": [0.0, 0.0] }       // อยู่กึ่งกลางพอดี ✅
```

### 5. Bottom-Center Elements (anchor_min: [0.5, 0.0])

**ก่อนแก้ไข** (ผิด):
```json
"controls_hint": { "anchored_position": [-180.0, -15.0] }  // Y ลบ = ลงไปด้านล่าง (นอกจอ)
```

**หลังแก้ไข** (ถูก):
```json
"controls_hint": { "anchored_position": [0.0, 15.0] }      // Y บวก = ขึ้นมาจากด้านล่าง ✅
```

## กฎการใช้ Anchored Position

### Anchor ด้านบน (Y = 1.0):
```
Anchor (Top)
    ↓ Y = -20 (ลงมา 20 pixels)
    ↓ Y = -50 (ลงมา 50 pixels)
    ↓ Y = -100 (ลงมา 100 pixels)
```

### Anchor ด้านล่าง (Y = 0.0):
```
    ↑ Y = 100 (ขึ้นไป 100 pixels)
    ↑ Y = 50 (ขึ้นไป 50 pixels)
    ↑ Y = 20 (ขึ้นไป 20 pixels)
Anchor (Bottom)
```

### Anchor กึ่งกลาง (Y = 0.5):
```
    ↑ Y = 100 (ขึ้นไป 100 pixels)
Anchor (Center) Y = 0
    ↓ Y = -100 (ลงมา 100 pixels)
```

## ผลลัพธ์

✅ Health Bar, Stamina Bar, Dash Indicator - แสดงที่ **top-left** ถูกต้อง
✅ FPS Counter, Position/Velocity Debug - แสดงที่ **top-right** ถูกต้อง
✅ Grounded, Wall Slide indicators - แสดงที่ **bottom-left** ถูกต้อง
✅ Controls Hint - แสดงที่ **bottom-center** ถูกต้อง
✅ DASHING! - แสดงที่ **center** ถูกต้อง

## Layout ที่ถูกต้อง

```
┌─────────────────────────────────────────────────────┐
│ Health Bar          FPS: 60                         │
│ Stamina Bar         X: 100 Y: 200                   │
│ Dash: 1             VX: 5.0 VY: 0.0                 │
│                                                      │
│                                                      │
│                    DASHING!                          │
│                                                      │
│                                                      │
│ GROUNDED                                             │
│ WALL SLIDE                                           │
│                                                      │
│         WASD: Move | Space: Jump | Shift: Dash      │
└─────────────────────────────────────────────────────┘
```

## ไฟล์ที่แก้ไข

- `projects/Celeste Demo/assets/ui/celeste_hud.uiprefab`
  - แก้ไข `anchored_position` ของทุก elements ให้ถูกต้อง

## การทดสอบ

1. ✅ แก้ไข prefab file
2. 🔄 รอทดสอบ: เปิด Game View
3. 🔄 รอทดสอบ: เปิด Prefab Editor
4. 🔄 รอทดสอบ: ตรวจสอบว่าตำแหน่งถูกต้องทั้งสองที่

---

**สถานะ**: ✅ COMPLETED - พร้อมทดสอบ!
