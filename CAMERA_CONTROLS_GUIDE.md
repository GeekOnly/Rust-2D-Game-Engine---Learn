# Camera Controls - คู่มือการใช้งาน

## 🎮 การควบคุมกล้องใน Scene Editor

### 📐 2D Mode

| การกระทำ | วิธีใช้ | คำอธิบาย |
|---------|---------|----------|
| **Pan (เลื่อนกล้อง)** | Middle Mouse + Drag | เลื่อนกล้องไปรอบๆ |
| | Right Mouse + Drag | เลื่อนกล้อง (Unity 2D style) |
| **Zoom (ซูม)** | Scroll Wheel | ซูมเข้า/ออก |
| | Shift + Scroll | ซูมเร็ว 3x |
| | Ctrl + Scroll | ซูมช้า 3x |

### 🎯 3D Mode

| การกระทำ | วิธีใช้ | คำอธิบาย |
|---------|---------|----------|
| **Orbit (หมุนรอบจุด)** | Alt + Left Mouse + Drag | หมุนกล้องรอบ object ที่เลือก |
| **Free Look (มองรอบ)** | Right Mouse + Drag | หมุนกล้องแบบ FPS |
| **Pan (เลื่อนกล้อง)** | Middle Mouse + Drag | เลื่อนกล้องไปรอบๆ |
| **Zoom (ซูม)** | Scroll Wheel | ซูมเข้า/ออก |
| | Shift + Scroll | ซูมเร็ว 3x |
| | Ctrl + Scroll | ซูมช้า 3x |
| **Fly (บิน)** | WASD (เมื่อ hover) | เคลื่อนที่แบบ FPS |
| | Right Mouse + WASD | เคลื่อนที่แบบ Unity |
| | Shift + WASD | เคลื่อนที่เร็ว 3x |
| | Ctrl + WASD | เคลื่อนที่ช้า 3x |

## 🎯 WASD Movement (แก้ไขแล้ว!)

### ✅ ตอนนี้ทำงานได้ 2 แบบ:

#### 1. แบบง่าย (Hover Mode)
```
1. เลื่อนเมาส์ไปเหนือ Scene View
2. กด W/A/S/D เพื่อเคลื่อนที่
   - W = ไปข้างหน้า
   - S = ถอยหลัง
   - A = ไปซ้าย
   - D = ไปขวา
3. ไม่ต้องกดเมาส์!
```

#### 2. แบบ Unity (Right Mouse + WASD)
```
1. กด Right Mouse Button ค้างไว้
2. กด W/A/S/D เพื่อเคลื่อนที่
3. ลากเมาส์เพื่อมองรอบ (Free Look)
4. เหมาะสำหรับการควบคุมที่แม่นยำ
```

### ⚡ Speed Modifiers

| Modifier | ความเร็ว | ใช้เมื่อ |
|----------|---------|----------|
| **ไม่กด** | 1x (ปกติ) | เคลื่อนที่ทั่วไป |
| **Shift** | 3x (เร็ว) | เคลื่อนที่ระยะไกล |
| **Ctrl** | 0.3x (ช้า) | ปรับตำแหน่งแม่นยำ |

### 📊 ตัวอย่างการใช้งาน

**เคลื่อนที่ปกติ:**
```
1. Hover เมาส์เหนือ Scene View
2. กด W เพื่อไปข้างหน้า
3. กด A/D เพื่อเลี้ยวซ้าย/ขวา
```

**เคลื่อนที่เร็ว:**
```
1. Hover เมาส์เหนือ Scene View
2. กด Shift + W เพื่อบินเร็ว
3. ปล่อย Shift เพื่อชะลอ
```

**ควบคุมแม่นยำ (Unity-style):**
```
1. กด Right Mouse Button ค้างไว้
2. ลากเมาส์เพื่อมองรอบ
3. กด W/A/S/D เพื่อเคลื่อนที่
4. กด Ctrl + W เพื่อเคลื่อนที่ช้าๆ
```

## 🎨 Preset Views (3D Gizmo)

คลิกที่แกน X/Y/Z ใน Scene Gizmo (มุมขวาบน) เพื่อ snap กล้องไปมุมมองที่กำหนด:

| แกน | มุมมอง | ใช้สำหรับ |
|-----|--------|-----------|
| **+X** | ด้านขวา | ดูด้านข้างขวา |
| **-X** | ด้านซ้าย | ดูด้านข้างซ้าย |
| **+Y** | ด้านบน | ดูจากบน (Top-down) |
| **-Y** | ด้านล่าง | ดูจากล่าง |
| **+Z** | ด้านหน้า | ดูจากหน้า (Front) |
| **-Z** | ด้านหลัง | ดูจากหลัง (Back) |

## 🔧 Technical Details

### Camera Movement Calculation
```rust
// Forward/Backward direction
let forward = glam::Vec2::new(
    yaw_rad.cos() * pitch_rad.cos(),
    yaw_rad.sin() * pitch_rad.cos()
);

// Left/Right direction
let right = glam::Vec2::new(
    yaw_rad.sin(), 
    -yaw_rad.cos()
);

// Apply movement
scene_camera.position += movement * speed;
```

### Speed Calculation
```rust
let base_speed = 2.0;
let speed_multiplier = if shift { 3.0 } else if ctrl { 0.3 } else { 1.0 };
let final_speed = base_speed * speed_multiplier * (distance / 100.0).max(0.5);
```

### Activation Conditions
```rust
// WASD works when:
// 1. In 3D mode
// 2. Right mouse button is held OR hovering over scene view
let is_right_mouse_down = response.ctx.input(|i| 
    i.pointer.button_down(egui::PointerButton::Secondary)
);

if scene_view_mode == Mode3D && (is_right_mouse_down || response.hovered()) {
    // Process WASD input
}
```

## 🐛 Troubleshooting

### WASD ไม่ทำงาน
1. ✅ ตรวจสอบว่าอยู่ใน **3D mode** (ไม่ใช่ 2D)
2. ✅ ตรวจสอบว่าเมาส์ **hover อยู่เหนือ Scene View**
3. ✅ ลองกด **Right Mouse + WASD** (Unity-style)
4. ✅ ตรวจสอบว่าไม่มี UI อื่นที่ capture keyboard input

### กล้องเคลื่อนที่ผิดทิศทาง
- WASD เคลื่อนที่ตามทิศทางที่กล้องมอง
- ลองหมุนกล้องด้วย Right Mouse + Drag ก่อน
- ทิศทาง forward/right คำนวณจาก camera rotation

### กล้องเคลื่อนที่เร็วเกินไป/ช้าเกินไป
- กด **Shift** เพื่อเร็วขึ้น (3x)
- กด **Ctrl** เพื่อช้าลง (0.3x)
- ความเร็วปรับตาม camera distance อัตโนมัติ

### Right Mouse + WASD ไม่ทำงาน
- ตรวจสอบว่ากด Right Mouse Button ค้างไว้
- ตรวจสอบว่าเมาส์อยู่ใน Scene View
- ลองใช้ Hover Mode แทน (ไม่ต้องกดเมาส์)

## 💡 Tips & Tricks

### 1. เคลื่อนที่เร็วในพื้นที่กว้าง
```
Shift + W = บินเร็วไปข้างหน้า
Shift + Scroll = ซูมเร็ว
```

### 2. ปรับตำแหน่งแม่นยำ
```
Ctrl + W/A/S/D = เคลื่อนที่ช้าๆ
Ctrl + Scroll = ซูมช้าๆ
```

### 3. มองรอบ Object
```
1. เลือก object ใน Hierarchy
2. กด Alt + Left Mouse + Drag
3. กล้องจะหมุนรอบ object
```

### 4. Reset Camera
```
1. คลิกที่แกนใน Scene Gizmo
2. หรือกด F เพื่อ focus ที่ object ที่เลือก
```

### 5. Fly Mode (Unity-style)
```
1. กด Right Mouse Button ค้างไว้
2. ใช้ WASD เคลื่อนที่
3. ลากเมาส์เพื่อมองรอบ
4. เหมือนเล่นเกม FPS!
```

## 📚 Related Features

- **Scene View Modes** - 2D/3D toggle
- **Grid Component** - Grid visualization
- **Transform Gizmos** - Move/Rotate/Scale tools
- **Selection** - Entity selection in scene

## ✅ Status: FIXED & WORKING

WASD camera movement ทำงานได้แล้วทั้ง 2 แบบ:
- ✅ Hover Mode (ง่าย, ไม่ต้องกดเมาส์)
- ✅ Right Mouse + WASD (Unity-style, แม่นยำ)

---

**พร้อมใช้งาน!** ลอง hover เมาส์เหนือ Scene View แล้วกด WASD 🎮
